//chapter 6, loops
/*
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(void)
{
//project 1 - find largest number inputted by user
float user_num, largest_num;
int exit_flag = 1;;
largest_num = 0.0f;

while (exit_flag != 0){
printf("enter a number...");
scanf("%f",&user_num);

if(user_num >= largest_num){
    largest_num = user_num;
}

if(user_num <= 0){
    exit_flag = 0;
}
}

printf("the largest number entered was %.2f",largest_num);

return 0;
}
*/


//chapter 5, selection statements
/*
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(void)
{
//project 1: count digits in a given number
int user_number;

printf("give me a number... must be positive and 4 or fewer digits");
scanf("%d",&user_number);

if(user_number >= 0 && user_number <= 9){
    printf("the number %d has 1 digit",user_number);
}
else if(user_number >= 10 && user_number <= 99){
    printf("the number %d has 2 digits",user_number);
}
else if(user_number >= 100 && user_number <= 999){
    printf("the number %d has 3 digits",user_number);
}
else{
    printf("the number %d has 4 digits",user_number);
}

return 0;
}

*/
